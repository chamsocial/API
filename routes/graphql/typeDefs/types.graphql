scalar DateTime

type User {
  id: ID!
  slug: String!
  username: String
}

type Post {
  id: ID!
  title: String!
  slug: String!
  created_at: DateTime
  comments_count: Int
  author: User
}
type postsInfoType {
  count: Int!
}

