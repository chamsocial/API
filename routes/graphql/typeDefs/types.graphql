scalar DateTime

type User {
  id: ID!
  slug: String!
  username: String
}

type Post {
  id: ID!
  title: String!
  slug: String!
  content: String
  created_at: DateTime
  comments_count: Int
  author: User
  comments: [Comment]!
}
type postsInfoType {
  count: Int!
}

type Comment {
  id: ID!
  created_at: DateTime
  content: String
  parent_id: Int
  author: User
  comments: [Comment]
}
